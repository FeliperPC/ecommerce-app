import { CheckoutClient } from "@/app/(app)/checkout/CheckoutClient";

export const metadata = {
  title: "Checkout | Furniture Shop",
  description: "Complete your purchase",
};

export default function CheckoutPage() {
  return <CheckoutClient />;
}
